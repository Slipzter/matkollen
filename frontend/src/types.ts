
export type Product =  {
    energi_kcal ? : String,
    fett_totalt_g? : String,
    kolhydrater_g ?: String,
    livsmedelsnamn  : String,
    protein_g ?: String
}

export type Nutrient = {
  name: String,
  color?: String,
  percentage?: number,
}